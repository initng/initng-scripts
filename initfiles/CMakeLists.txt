SUBDIRS(daemon net system service runlevel)

IF(INSTALL_DEBUG)
	ADD_SUBDIRECTORY(debug)
ENDIF(INSTALL_DEBUG)

FILE(GLOB S_FILES "*.s")

PROCESS_SFILES(SERVICE_FILES ${S_FILES})

INSTALL_FILES(${SYSCONF_INSTALL_DIR}/initng FILES ${SERVICE_FILES})

